{# 
/**
 * Custom template for iterating over embedded video links
 * 
 */
 #}
{# <ul> #}
  {# {% for item in items %} #}
    {# <pre> #}
      {# {{ dump(items, attributes) }} #}
      {# {{ dump(item) }} #}
    {# </pre> #}
    {# <iframe width='100%' height='315' src='{{ item }}'></iframe> #}
  {# {% endfor %} #}
{# </ul> #}